/**查询功能模块 */
// eslint-disable-next-line
import api from "@/api/query";

const query = {
  namespaced: true,
  state: {},
  mutations: {},
  actions: {
    //通过action发起api请求
  }
};

export default query;
